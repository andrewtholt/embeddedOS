
#include "msg.h"

bool addToPool() {
}

struct msg *mkMsg(const threadId s, const cmdType c, const char *k, const char *v) {
}

bool initPool() {
}

struct msg *getMsg() {
    struct msg *ptr = NULL;

    return(ptr);
}
